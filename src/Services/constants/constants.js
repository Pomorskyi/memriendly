const DEFAULT_PATH = '/'
const LOGIN_PATH = '/login'
const HOME_PATH = '/home'

const NO_PHOTO_CHOOSEN_PATH = '/images/noavatar.png'

// eslint-disable-next-line import/no-anonymous-default-export
export default {
    DEFAULT_PATH,
    LOGIN_PATH,
    HOME_PATH,
    NO_PHOTO_CHOOSEN_PATH
}